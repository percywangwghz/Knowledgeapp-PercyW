# 部署说明（本地运行）

本仓库设计为**下载后在本地运行**，AI 功能需要国内网络环境（Moonshot API 限制境外 IP），
因此不推荐部署到海外云平台（Streamlit Cloud 等服务器在境外，AI 功能会被 API 拒绝）。

## 一、下载

GitHub 仓库页面 → 绿色 **Code** 按钮 → **Download ZIP** → 解压到任意位置。
（或 `git clone <仓库地址>`）

解压到哪儿都能跑，无需改任何配置——应用自动读取自己目录下的 `knowledge/` 文件夹。

## 二、启动

**Windows**：双击 `start.bat`。

- 首次运行自动创建虚拟环境并安装依赖（约 1-3 分钟，需联网）
- 之后每次双击直接启动，浏览器自动打开 http://localhost:8501
- 关闭黑色命令行窗口 = 停止运行

**macOS / Linux**（或 Windows 手动方式）：

```bash
python3 -m venv .venv
.venv/bin/pip install -r requirements.txt
.venv/bin/streamlit run app.py
```

要求 Python 3.10 或更高版本。

## 三、使用前必做：填 API Key

打开页面后，在**左侧边栏粘贴自己的 Moonshot API Key**
（[platform.moonshot.cn](https://platform.moonshot.cn) 注册领取）。

- Key 只保存在你自己电脑（`data/local_api_key.txt`），不会上传
- AI 功能（Radar / 技术提取 / 归档 / 评审 / Battle）走你自己的额度
- 不填 Key 也能正常浏览所有文档，只是 AI 功能不可用

补充：如果你直接命令行跑脚本（`python radar_auto.py` 等，不经过网页），
key 的读取顺序是 环境变量 `MOONSHOT_API_KEY` > `~/.kimi/config.toml`
（装了 Kimi CLI 的机器会复用它的 key）。网页端只用侧边栏填的 key，不受影响。

## 四、数据都在哪

- 知识库 = 包内 `knowledge/` 文件夹，你归档/提取/周报产出的文档也写在这里
- 整个文件夹拷走 = 完整备份
- `data/` 是本机运行状态（key、任务进度等），无需备份

## 五、常见问题

| 现象 | 解决 |
|------|------|
| 提示"未找到 Python" | 安装 [Python 3.10+](https://www.python.org/downloads/)，安装时勾选 **Add python.exe to PATH** |
| 提示"没有找到可用的 Python 3.10+"，但已装过 Python | 多半是微软商店占位程序干扰：设置 → 应用 → 高级应用设置 → 应用执行别名，关闭 python.exe / python3.exe 的"应用安装程序"别名；或确认装的版本 ≥ 3.10 |
| 提示"虚拟环境创建失败" | 看窗口里 Python 打印的具体原因：① 杀毒软件拦截 .venv 创建，暂时关闭重试；② 文件夹无写入权限（Program Files、网盘同步目录），移到普通目录；③ 装的是嵌入式绿色版 Python（不支持 venv），换装官网安装版 |
| AI 功能灰色或报错 | 侧边栏没填 Key，或 Key 已失效 |
| 8501 被占用 | Streamlit 会自动换 8502，看终端里打印的实际地址 |
| 依赖安装失败 | 检查网络后重新双击 `start.bat` |
