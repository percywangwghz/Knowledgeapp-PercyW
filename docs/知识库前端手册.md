# 一级投研知识库 · 前端手册（结构 + 线框 + 组件清单）

> **v2 重构记录（2026-08-21）**：Streamlit 端已按 `docs/一级投研知识库_前端重构设计说明书_v2.md` 完成重构——
> 新增 `jobs.py`（中枢读取模块）与"控制台 / Console"页（view_mode=`console`，统一展示 radar/tech/review/ingest/battle
> 全部后台 job + Battle 会话 + AI 成本，任务列表 3 秒 fragment 自刷）；新增 `data/view_history.json` 最近访问记录，
> 打开文档统一走 `open_doc()`；IA 改为 Sidebar 三组（WORKSPACE / KNOWLEDGE / RECENT + 底部 Settings 抽屉）
> + 顶栏 Header（品牌 + 全局搜索 + 任务状态）；首页改为 Research Home（RECENT / COLLECTIONS / RECENT CHANGES）；
> 阅读页改为三栏 Reading Workspace（Context Rail + 正文 + TOC）+ Zen 模式 + 2px 阅读进度条；
> 主区宽度 1280 → 1440px，全部色值换 v2 token。**本手册正文描述的是旧版结构，阅读时以本节为准。**
>
> **v2 第二轮：对齐 demo 视觉（2026-08-21，以 `ui_demo_v2/index.html` 为准）**：
> - **Console 页删除，改右侧 fixed 任务抽屉**：Header「● N tasks / Tasks」按钮 toggle
>   （`session_state.task_drawer_open`），抽屉是 `bordered container + .kb-drawer-marker`，CSS 用
>   `div[data-testid="stLayoutWrapper"]:has(.kb-drawer-marker)` 定位 fixed 右侧 400px（z-index 1000002，
>   Battle 写回预览变体 `.kb-drawer-preview` 1000003）；任务卡 `.task-card`（feature + 状态 pill +
>   进度条 + 当前步骤 + 已用时/ETA），`_drawer_tasks_fragment = st.fragment(run_every=3)` 自刷；
>   done/error 任务只留 6h（`_job_visible_in_drawer`），中断任务"已用时"以 job 文件 mtime 为终点。
>   AI 成本（本周用量）挪进 sidebar Settings expander 底部；Battle 会话区随 Console 一并删除。
> - **展示页全改 demo 纯 HTML 行**：`doc_row_html / lib_row_html / change_row_html / result_row_html`
>   （`_snippet` 做 `<mark>` 高亮）+ 重写的 `render_home / render_category_view / render_search_results /
>   render_industry_cognition`；阅读页标题 `.reader-title`（serif 30px）+ `.doc-meta-line`（分类 · 赛道 ·
>   状态 pill · Updated）。**两个坑：① `<a>` 内只能放 inline `<span>`，放块级 `<div>` 会被 markdown-it
>   从行内段落拆出来、行结构散架；② st.markdown 会把无 target 的 `<a>` 重写成 `target="_blank"`，
>   所有站内锚点必须显式写 `target="_self"`，否则点击开新标签页（新 session）。**
> - **query_params 导航**：展示页 HTML 行输出 `<a href="?doc=..."/"?cat=..."/"?nav=...">`（`doc_href()/
>   cat_href()`，urllib quote），整页刷新后由 session 初始化处的路由段消费参数、落 session_state、
>   `st.query_params.clear()`。阅读页 / Battle 页用 `.page-wide-marker` 撑宽主区（默认 max-width 1080px）。
> - **battle.py `render_battle` 按 demo 三栏辩论室重写**（1–411 行后台逻辑原样）：新增 `_parse_battle_state`
>   （解析文档 `## ⚔️ Battle 记录` 节 → 状态行/关键假设表/证伪条件/反向世界）、`_current_phase`（最后一条
>   AI 消息的 `【Step N · 名】`→ASSUMPTION ATTACK 等）、`_entry_html`（demo `.battle-entry`，mono `#NN`
>   序号——消息无时间字段）、`_confidence_html`（≥70 High / ≥40 Medium）、`_thesis_context_html /
>   _battle_state_html`；三栏 `st.columns([2.4,6.6,2.4])` + 左右栏线 marker；左栏顶 selectbox 用
>   「值先行」模式（先读 `session_state.battle_doc` 再画 widget）；写回预览改右侧 fixed 抽屉
>   （Approve=`_confirm_writeback` / Reject=pop+rerun）；quiet 按钮行 + chat_input + `_render_reply_live`
>   打字机保留。
> - 残留清理：隐藏 `stAppDeployButton`（注意正确 testid）、禁用态 primary 按钮浅底灰字、TOC active
>   链接压盖、sidebar 分隔线换 spacer。
>
> **v2 第三轮（2026-08-22，Radar/Review 重排 + Header 导航 + 侧栏加宽）**：
> - **Radar 按 demo §11 重排**：`st.tabs` 换 `.radar-nav` 纯 HTML 文字 tab（`<a href="?nav=radar&rtab=...">
>   target="_self"`，路由段消费 `rtab` → `session_state.radar_tab`）；六个子页函数 `_render_overview
>   （抓取 quiet 按钮 + job 状态 + TODAY 三个 mono 大数字（近 7 天 signals/themes/variables）+ SIGNAL STREAM
>   行式 `.signal-row` + NARRATIVE CHANGES `.narrative-block`（近 30 天历史条目，转变=accent/微调=warning
>   左边线）+ MARGINAL VARIABLES `.var-chip`）/ _render_signals（完整列表+主题筛选+行尾 × 删除）/
>   _render_themes（`.theme-article` 叙事长文：Current Narrative/Supporting Evidence/Representative
>   Views/Market Consensus/Core Disagreement/Marginal Variables/Narrative History，多行字段
>   `_evidence_html` 自动转 `<ul>`）/ _render_variables（静态 `div.lib-row` 完整列表）/ _render_reports
>   （历史周报 doc-row + 生成器）/ _render_sources（公众号技术提取原逻辑）`。后台 job 线程、
>   tech 提取/合并、周报写回函数全部未动。
> - **Review 按 demo §10 重排**：`.steps-flow` 五步进度条（无草稿=01 PROJECT / 有草稿=04 REVIEW /
>   刚提交=05 COMMIT，`_review_just_committed` 一次性标记）；选择字段加 `.review-field` 标签；
>   「Generate Assessment」接原有 `_start_job`；生成后两栏 `.assessment-cols`——左 EXISTING KNOWLEDGE
>   （`_industry_snapshot` 从行业总文档尽力解析 行业阶段/行业坐标/核心变量表，无对应章节显示「—」）、
>   右 AI ASSESSMENT（项目判断/增量思考保留 text_area 可编辑 + 坐标落位/变量更新/反哺/阶段更新
>   `.assess-block`）；Commit Preview 用 `.diff-block`（+/~ 行，项目文档已有 🧭 节时第一行自动
>   从「新增」变「替换」）；Back 丢草稿 / Commit to Library 走原有双文档写回；「下一步发起 Battle」
>   联动保留。job worker、写回函数、preselect/搜索过滤未动。
> - **Battle**：三栏 [2.4,6.6,2.4] → [2.8,6.2,2.8]，侧栏文本 `overflow-wrap:anywhere` 自然换行；
>   写回审计修两处：`_confirm_writeback` 改为成功后才丢弃预览（写回失败可重试 Approve）；
>   「清空」按钮连同 `battle_preview`/`battle_finalize` 一并清除（防清空对话后旧预览被误批）。
> - **Header 加文字导航**：品牌 + 首页/论文之战/投资雷达/新项目评审/文件归档 + 窄搜索胶囊
>   （max-width 300px）+ Tasks 胶囊 + ● Indexed。Header 内按钮默认压成文字导航样式（无框、
>   accent 下划线表当前页，`.hdr-nav-on` marker 兄弟选择器；Tasks 用 `.hdr-pill-marker` 恢复胶囊）。
>   sidebar WORKSPACE 组移除（KNOWLEDGE/RECENT/Settings 保留）。
> - **Sidebar 加宽**：224px → 31.5rem（504px），Header fixed 层 `left` 14rem → 31.5rem 联动
>   （收起态 3rem 不变）。
>
> **v2 第四轮（2026-08-22，阅读页两栏化 + 滚动劫持修复）**：
> - **三栏 [2.2,6.8,1.9] → 两栏 [7.4,2.6]**：Context Rail 与 TOC 合并为单一右栏（顺序：
>   Contents 目录 → Document Context → Related → Actions），正文占主区宽度 62% → 74%。
> - **修复「无法下拉、不断跳回顶部」**：根因是 TOC spy（`_TOC_SPY_JS`）的 `keepInView()`
>   从 active 目录链接向上找「最近可滚动祖先」并滚它——TOC 在主内容流里时该祖先就是
>   整页容器，300ms 轮询不停把页面拽回顶部。修法：右栏 `stColumn` 加 `.rail-marker`，
>   CSS `div[data-testid="stColumn"]:has(.rail-marker) { position: sticky; top: 72px;
>   align-self: flex-start; max-height: calc(100vh - 96px); overflow-y: auto; }`——
>   keepInView 只滚右栏自身；注意必须 `align-self: flex-start`，否则 stColumn 被父 flex
>   拉伸、sticky 失效。
> - 目录/相关推荐「间歇性消失」实测为冷启动 rerun 撕裂采样假象，暖机后 6/6 稳定
>   （Playwright 验证：TOC 30 条、滚动 set 1200 保持 1200、锚点点击高亮跟随、
>   首页 RECENT 按钮 → 文档页全链路正常）。
> - **面包屑回跳索引恢复**：阅读页正文列顶部 + 分类页页头渲染 `.crumb`
>   （`_crumb_html(items)`，「知识 › 分类 › 当前文档」，前两级为 `?nav=home` / `?cat=...`
>   链接走 query_params 路由回跳，末级纯文本）；Zen 模式不渲染。
> - **全站宽度拉齐**：首页 / 文件归档（ingest.py）/ 新项目评审（review.py）/ 分类页 /
>   搜索页补 `.page-wide-marker`（此前只有 reader/battle/radar 撑宽，其余留在 1080px
>   与 Header 不对齐）；首页大搜索胶囊 `a.big-search` 去掉 `max-width: 640px`，
>   与下方 RECENT 行同宽对齐。
> - **Battle 中栏改聊天室布局**：对话区（空态/消息流/流式打字机）包进 `st.container()`
>   滚动框——`battle-msgs-marker` 放在容器【外】紧邻其前，CSS 用相邻兄弟选择器
>   `stElementContainer:has(.battle-msgs-marker) + stLayoutWrapper` 只打容器
>   （1.58 里 st.container 外壳是 stLayoutWrapper；标记放容器内会让 :has 同时命中
>   容器与标记自身两层，标记行抢 flex 空间）；整页 flex 链
>   `stMainBlockContainer(flex 列, 100vh) > stVerticalBlock(flex:1) > stLayoutWrapper(flex:1)
>   > stHorizontalBlock(flex:1, min-height:0)`，中栏内层 `height:100%` flex 纵列，
>   操作区按钮行 + chat_input 靠 dock-marker `margin-top:auto` 吸底；左右栏
>   `overflow-y:auto` 各自框内滚动，整页不滚动。空态在框内垂直居中
>   （`:has(.empty-state)` 的 elementContainer `margin: auto 0`）。
>   **对话区高度写死 `58vh`（min 280px，flex:none）——生成文字时框不变高，
>   内容框内手动滚动；按用户要求移除了贴底跟随脚本。**
>   另：全局 `padding-top` 4.5rem → 3.3rem（所有页面功能区贴近顶栏，Battle 不再单独覆写）。

> 整合版，一份喂给 GPT 即可。覆盖两套前端：
> **A. 静态只读站**（`build_html.py` → `dist/`）与 **B. Streamlit 交互版**（`app.py` 等）。
> 已按 2026-08-21 最新代码核对（`dist/` 目录是 2026-07-27 的旧产物，一切以脚本为准）。
> 每节标注代码位置，方便改写时定位。

---

# 0. 两套前端总览

| | A. 静态只读站 | B. Streamlit 交互版 |
|---|---|---|
| 代码 | `build_html.py`（960 行） | `app.py`（1600 行）+ `battle.py` / `review.py` / `radar.py` / `ingest.py` |
| 产物/入口 | `dist/*.html`，`file://` 双击可用 | `localhost:8501` |
| 职责 | 只读浏览、本地全文搜索 | AI 交互（评审/Battle/Radar/归档）、写回知识库 |
| 技术 | 零框架零 CDN，f-string 拼 HTML | Streamlit，设计 token 见 B10 |

```text
~/.kimi/knowledge/**/*.md
  └─ indexer 提取元数据
       ├─→ data/kb_index.json ──→ app.py（Streamlit，交互读写）
       └─→ build_html.main()：
            · BASE_CSS                    → dist/assets/style.css
            · documents（含全文）→ JSON   → dist/assets/search.js（1MB 本地索引）
            · markdown 渲染正文           → dist/docs/*.html
            · data/radar_*.json           → dist/radar.html
            · 正则抽文档章节              → dist/battle.html / compare.html

上游写入方：app.py（评审/Battle 写回 md）、radar_auto.py（写 radar_*.json）、
ingest.py（归档）。静态站只是这些数据的只读投影。
```

⚠️ 验证静态站改动必须重跑：`venv/Scripts/python.exe knowledge_app/build_html.py`

---

# Part A · 静态只读站（build_html.py → dist/）

## A1. 站点地图

| 页面 | 文件路径 | 生成函数（build_html.py） |
|---|---|---|
| 首页 | `dist/index.html` | `build_home()` :465 |
| 搜索 | `dist/search.html?q=...` | `build_search()` :617 |
| 新项目评审 | `dist/compare.html` | `build_compare()` :637 |
| Thesis Battle | `dist/battle.html` | `build_battle()` :763 |
| Investment Radar | `dist/radar.html` | `build_radar()` :790 |
| 分类页 ×8 | `dist/category/<cat_key>.html` | `build_category()` :512 |
| 文档页 ×N | `dist/docs/<分类>__<篇名>.html` | `build_doc()` :547 |
| 资源 | `dist/assets/style.css`、`dist/assets/search.js` | `main()` :916-932 |

URL 规则：`doc_url()` :52 把 `02_deals/xxx.md` → `docs/02_deals__xxx.html`；中文文件名直接进 URL；深度 1 的页面链接加 `../` 前缀。

## A2. 全局框架（`page_html()` :439，无 footer）

```text
┌─────────────────────────────────────────────────────────────────────────┐
│┌────────────┬─────────────────────────────────────────────────────────┐ │
││            │  TopBar：标题 + 搜索框                                   ││
││  Sidebar   ├─────────────────────────────────────────────────────────┤ │
││  250px     │                                                         │ │
││  sticky    │              各页面 main 内容区                          ││
││            │              (max-width 1100px)                         │ │
││            │                                                         │ │
│└────────────┴─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────┘

响应式：唯一断点 900px，Sidebar 变为顶部通栏，grid 降列。

```

### A2.1 Sidebar（`sidebar_html()` :391）

```text
┌─────────────────────┐
│ 导航                │
│  🏠 首页            │
│  🧭 新项目评审      │
│  ⚔️ Thesis Battle   │
│  📡 Investment Radar│
│ ──────────────────  │
│ 分类                │
│  🏭 行业认知   (6)  │
│  🔬 项目解剖  (31)  │
│  🛠️ 方法论     (4)  │
│  📊 横向比较   (3)  │
│  📈 动态追踪   (2)  │
│  🎯 投资策略   (1)  │
│  💡 经验沉淀  (17)  │
│  💼 被投基金   (2)  │
│ ──────────────────  │
│ 本页目录（仅文档页）│
│  · h2 锚点          │
│  · h3 锚点（限30条）│
│ ──────────────────  │
│ 快捷入口            │
│  ⚡ 固定文档 ×4     │
│ ──────────────────  │
│ 统计                │
│  文档 65 · 分类 8   │
└─────────────────────┘

```

- 分类顺序 `CATEGORY_ORDER`（:26），当前分类 `.active` 高亮
- 快捷入口 `QUICK_LINKS`（:30）写死 4 篇，文件存在才渲染（⚡ 前缀）
- **问题**：Sidebar 无折叠/分组，内容多了会失控

### A2.2 TopBar（`topbar_html()` :426）

```text
┌────────────────────────────────────────────────────────────────────┐
│ 🧠 一级投研知识库                                                  │
│ 沉淀认知 · 关联洞察 · 复用框架      ┌──────────────┐ ┌──────┐      │
│                                   │ 搜索…        │ │ 搜索 │        │
│                                   └──────────────┘ └──────┘        │
└────────────────────────────────────────────────────────────────────┘

```

- 搜索表单 GET 到 `search.html?q=...`，placeholder：「搜索文档、项目、赛道、概念... 例如：AI4S、光掩模」

## A3. 首页（`build_home()` :465，已核对最新脚本）

```text
┌────────────────────────────────────────────────────────────────────┐
│ 知识库概览                                                         │
│ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐                    │
│ │    65       │ │     8       │ │  2026-08-21 │                    │
│ │  总文档     │ │   分类      │ │  生成时间   │                    │
│ └─────────────┘ └─────────────┘ └─────────────┘                    │
│ （.metric-card ×3，3 列 grid）                                     │
│                                                                    │
│ 按分类浏览                                                         │
│ ┌──────────────────────────┐ ┌──────────────────────────┐          │
│ │ ▔▔ 4px 分类色条          │ │ ▔▔ 4px 分类色条          │          │
│ │ 🏭 行业认知           6篇│ │ 🔬 项目解剖          31篇│          │
│ │ 分类描述……               │ │ 分类描述……               │          │
│ │  · 文档链接 1            │ │  · 文档链接 1            │          │
│ │  · …（前 5 篇）          │ │  · …（前 5 篇）          │          │
│ │ 查看全部 6 篇 →          │ │ 查看全部 31 篇 →         │          │
│ └──────────────────────────┘ └──────────────────────────┘          │
│ （.cat-card ×8，2 列 grid，色条用 CATEGORY_COLORS tab10 色系）     │
│                                                                    │
│ 最近更新                                                           │
│ ┌──────────────────────────┐ ┌──────────────────────────┐          │
│ │ doc-card（标题/摘要/meta）│ │ doc-card                 │         │
│ └──────────────────────────┘ └──────────────────────────┘          │
│ （.doc-card ×10，按修改时间倒序）                                  │
└────────────────────────────────────────────────────────────────────┘

```

## A4. 文档卡片（`doc_card()` :100，全站复用的核心组件）

```text
┌────────────────────────────────────────────┐
│ 文档标题                                   │
│ 副标题 / 摘要（md 首个 > 引用）            │
│ 🏭 行业认知 · 跟踪中 · 2026-07-20          │
└────────────────────────────────────────────┘

```

- 带 `data-track / data-status / data-modified / data-name` 属性（供 deals 筛选 JS）
- 出现位置：首页最近更新、分类页列表、搜索结果、相关文档推荐

## A5. 分类页（`build_category()` :512）

```text
┌────────────────────────────────────────────────────────────────────┐
│ 面包屑：首页 / 项目解剖                                            │
│ ▔▔▔▔ 分类色条                                                      │
│ 🔬 项目解剖                                                  31 篇 │
│ 分类描述 · 共 31 篇                                                │
│                                                                    │
│ 【仅 02_deals 有筛选条】（DEALS_FILTER_JS :352）                   │
│ ┌──────────────────────────────────────────────────────────────┐   │
│ │ [全部赛道▾]  [全部状态▾]  [按更新时间▾]        命中 N 篇     │   │
│ └──────────────────────────────────────────────────────────────┘   │
│                                                                    │
│ ┌──────────────────────────────┐ ┌──────────────────────────────┐  │
│ │ doc-card                     │ │ doc-card                     │  │
│ └──────────────────────────────┘ └──────────────────────────────┘  │
└────────────────────────────────────────────────────────────────────┘

```

- 筛选是纯前端 DOM 显隐 + 排序，读写卡片 `data-*` 属性
- 赛道/状态来自 `tracks.py` 的关键词推断与状态归一

## A6. 文档页（`build_doc()` :547）

```text
┌────────────────────────────────────────────────────────────────────┐
│ 面包屑：首页 / 项目解剖 / 伏塔_AI驱动电池研发平台                  │
│                                                                    │
│ 文档标题                                                           │
│ 🔬 项目解剖 · 📅 2026-07-15 · 🏷️ 推进中 · 📌 伏塔                  │
│ ──────────────────────────────                                     │
│ 【正文 .doc-body.md，markdown 渲染】                               │
│ 表格（.table-wrap 斑马纹+横滚）、blockquote 蓝条、                 │
│ 图片（限 70% 宽 / 420px 高）、ASCII 表格（<pre> 等宽字体）         │
│ ──────────────────────────────                                     │
│ ┌──────────────────────┐ ┌──────────────────────┐                  │
│ │ ← 上一篇             │ │ 下一篇 →             │                  │
│ └──────────────────────┘ └──────────────────────┘                  │
│ ▸ 🔗 相关文档推荐（<details>，打分取前 5，indexer.py:294）         │
│ ▸ 📎 文件信息（<details>：文件名/路径/大小/修改时间）              │
│ ↑ 回到顶部                                                         │
└────────────────────────────────────────────────────────────────────┘

```

- 正文首个 `#` 标题被剥掉，h2/h3 生成 TOC 进左侧 Sidebar

## A7. 搜索页（`build_search()` :617 + SEARCH_JS :305）

```text
┌────────────────────────────────────────────────────────────────────┐
│ 面包屑：首页 / 搜索                                                │
│ 【 #summary 】找到 3 篇相关文档                                    │
│ 【 #results 】                                                     │
│ ┌──────────────────────────────────────────────────────────────┐   │
│ │ doc-card（标题命中词高亮 <mark>）                            │   │
│ └──────────────────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────────────────┘

```

- 页面只有两个空容器，渲染全靠 `assets/search.js`（1MB = 全文索引 `KB_DOCS` + 检索逻辑）
- 打分权重：title×10 / project×6 / name×5 / category×4 / content×3；纯本地，`file://` 可用

## A8. 新项目评审 compare 页（`build_compare()` :637，纯静态无 JS）

```text
┌────────────────────────────────────────────────────────────────────┐
│ ① 赛道总览                                                         │
│ ▸ 光计算（<details> 按 track 分组）                                │
│   ┌──────────┬────────┬────────────┬──────────────────────┐        │
│   │ 项目     │ 状态   │ 更新时间   │ 一句话定位           │        │
│   │ 光本位   │ 推进中 │ 2026-07-15 │ PCM 光计算芯片       │        │
│   └──────────┴────────┴────────────┴──────────────────────┘        │
│ ② 行业坐标与关键变量                                               │
│ ┌──────────────────────────────────────────────────────────────┐   │
│ │ 正则 COORD_SECTION_RE :629 抽 01_industry 对应章节渲染       │   │
│ └──────────────────────────────────────────────────────────────┘   │
│ ③ 项目评审留档                                                     │
│ ┌──────────────────────────────────────────────────────────────┐   │
│ │ 正则 REVIEW_SECTION_RE :630 抽 02_deals「🧭 行业总文档评审」│    │
│ └──────────────────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────────────────┘

```

## A9. Thesis Battle 页（`build_battle()` :763，纯静态无 JS）

```text
┌────────────────────────────────────────────────────────────────────┐
│ ┌──────────────────────────────────────────────────────────────┐   │
│ │ ⚔️ Battle 记录（BATTLE_SECTION_RE :724 抽章节，按时间倒序） │    │
│ └──────────────────────────────────────────────────────────────┘   │
│ 【当前实际状态：0 篇 → 空态卡】                                    │
│ ┌──────────────────────────────────────────────────────────────┐   │
│ │ 暂无 Battle 记录                                             │   │
│ │ 🔒 交互式 Battle 请使用 Streamlit 版（8501）                 │   │
│ └──────────────────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────────────────┘

```

## A10. Investment Radar 页（`build_radar()` :790，纯静态无 JS）

```text
┌────────────────────────────────────────────────────────────────────┐
│ 最近抓取：2026-07-27 08:15（radar_auto.log 最后一行）              │
│ ① 信号池（radar_signals.json 前 50 条 → .cmp-table）               │
│ ┌──────┬──────┬──────┬──────────┬────────┬────────────────┐        │
│ │ 日期 │ 来源 │ 主题 │ 事件类型 │ 标题   │ 为什么改变认知 │        │
│ └──────┴──────┴──────┴──────────┴────────┴────────────────┘        │
│ ② 主题叙事（radar_themes.json，每主题一张卡）                      │
│ ┌──────────────────────────────────────────────────────────────┐   │
│ │ 当前叙事 / 支持证据 / 代表性观点 / 共识 / 分歧               │   │
│ │ ▸ 叙事演变历史（<details>）                                  │   │
│ └──────────────────────────────────────────────────────────────┘   │
│ ③ 边际变量（radar_variables.json，卡片）                           │
│ ┌──────────────────────────────────────────────────────────────┐   │
│ │ 新增信息 / 原有预期 / 预期变化 / 边际变量 / 市场影响         │   │
│ └──────────────────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────────────────┘

```

## A11. 组件清单（静态站）

```text
Layout:   AppShell（.layout）/ Sidebar / TopBar + SearchForm / Main
Content:  DocCard（带 data-*，全站复用）/ MetricCard / CategoryCard（色条）
          Breadcrumb（各页手写）/ Details（原生 <details>）/ PrevNext
Data:     CmpTable / MdTable（.table-wrap 斑马纹横滚）/ Toc / FilterBar
          SearchResult（#summary + #results + <mark>）
Feedback: EmptyState（仅 battle 页手写一个）/ ErrorState 无 / Toast 无
```

样式 token（BASE_CSS :137-303）：

```css
:root {
  --kb-bg: #fafaf9;  --kb-card: #fff;   --kb-text: #1c1c1e;
  --kb-accent: #3b6ea5;  --kb-accent-soft: #eaf1f8;
  --kb-border: #e5e3df;
  /* 圆角 8px / 侧栏 250px / 主区 1100px / 断点 900px */
}
/* 分类色：CATEGORY_COLORS（tab10 色系），内联在 .cat-strip */
```

---

# Part B · Streamlit 交互版（app.py + battle/review/radar/ingest）

## B1. 功能区总览

```text
Streamlit 应用（localhost:8501，单页 app，view_mode 路由）
│
├── 顶部导航（fixed 顶栏，5 项）
│   ├── 首页 ────────┬── 分类页（含 02_deals 筛选）
│   │                ├── 搜索结果页
│   │                └── 文档详情页（阅读模式 + TOC 滚动跟随）
│   ├── 论文之战     → battle.py   AI 红队对抗 + 写回审批
│   ├── 投资雷达     → radar.py    5 个 Tab（采集/叙事/变量/周报/公众号）
│   ├── 新项目评审   → review.py   AI 评审草稿 + 双文档写回（下方嵌入行业认知列表）
│   └── 文件归档     → ingest.py   上传 → AI 分类整理 → 入库
│
└── 侧边栏（常驻）
    ├── API 设置（厂家/端点/模型/Key，本地持久化）
    ├── 分类导航
    ├── 本页目录（仅文档模式，JS scroll-spy）
    ├── 快捷入口（5 个固定文档）
    └── 统计
```

视图路由：`st.session_state.view_mode` ∈ home / category / search / doc / battle / radar / compare / ingest（app.py:1584-1599 分发）。

## B2.1 顶部导航条（app.py:1315-1337）

```text
┌────────────────────────────────────────────────────────────────────┐
│ 一级投研知识库  首页  论文之战  投资雷达  新项目评审  文件归档     │
│                 ▔▔（当前页：文字加深 + 2px Navy 下划线）           │
└────────────────────────────────────────────────────────────────────┘

```

- 真 Streamlit 按钮，靠 CSS `:has(.topnav-marker)` + `position:fixed` 浮到顶栏（无 JS 注入）
- 「首页」在浏览类子视图（category/search/doc）下保持高亮

## B2.2 侧边栏（app.py:1371-1576）

```text
┌───────────────────────┐
│ API 设置              │
│  API 厂家: [moonshot▾]│
│  端点: https://…      │
│  模型: [预设默认]     │
│  API Key: [••••••••]  │
│  ✓ Key 已保存在本机   │
│ ────────────────────  │
│ 分类                  │
│  项目解剖 · 31        │
│  方法论 · 4           │
│  横向比较 · 3         │
│  动态追踪 · 2         │
│  投资策略 · 1         │
│  经验沉淀 · 17        │
│  被投基金 · 2         │
│  技术沉淀 · N         │
│ ────────────────────  │
│ 本页目录（仅文档模式）│
│  · 章节A  ← 滚动跟随  │
│  · 章节B    高亮当前  │
│ ────────────────────  │
│ 快捷入口              │
│  📖 使用手册          │
│  AI4S项目矩阵         │
│  项目解剖模板         │
│  科学家创业评估       │
│  产业链投资图谱       │
│ ────────────────────  │
│ 统计                  │
│  文档: 65  分类: 8    │
└───────────────────────┘

```

- 厂家下拉标注「可联网搜索」（雷达才可用）；custom/anthropic 需手填 Base URL
- Key 密码框，填入即写本机 `data/local_api_key.txt`，启动自动载入；厂家/模型/端点同样持久化
- 本页目录：`components.html` 注入 JS scroll-spy（app.py:1472-1548），滚动高亮当前章节 + 目录自动跟随；上限 30 条；`<details>` 块标题不进目录

## B3. 首页（`render_home()` app.py:1002）

```text
┌──────────────────────────────────────────────────────────────────────┐
│ 一级投研知识库                                                       │
│ 沉淀认知 · 关联洞察 · 复用框架                                       │
│ ┌──────────────────────────────────────────────┐ ┌────┐              │
│ │ 搜索文档、项目、赛道、概念… 例如：AI4S      │ │刷新│               │
│ └──────────────────────────────────────────────┘ └────┘              │
│                                                                      │
│ 概览（stat-flex 三栏，细线分隔，无卡片）                             │
│  知识库概览      │  本周 AI 花费     │  按功能细分                   │
│  总文档     65   │  总花费   ¥12.30  │  功能      调用 Tokens 花费   │
│  分类        8   │  调用次数 45      │  Battle    12  8.2万 ¥5.10    │
│  索引时间 07-27  │  Tokens  32万     │  Radar     20 15.4万 ¥6.20    │
│                  │                   │  （数据：data/ai_costs.jsonl）│
│                                                                      │
│ 按分类浏览（2 列，行式排版，细线分隔）                               │
│  项目解剖 31篇                │  方法论 4篇                          │
│  一级框架：公司深度…          │  可复用工具…                         │
│   · 文档标题（前 5 篇）       │   · 文档标题                         │
│   查看全部 31 篇 →            │                                      │
│                                                                      │
│ 最近更新（doclist 行式列表，10 条）                                  │
│  文档标题                                                    ✎       │
│  项目解剖 · 光计算 · 2026-07-20 · 推进中                             │
│  副标题摘要……                                                        │
│ ─────────────────────────────────────────────────────────            │
└──────────────────────────────────────────────────────────────────────┘

```

- 「刷新」= `build_index(force=True)` 重建索引
- AI 花费来自 `data/ai_costs.jsonl` 近 7 天聚合（`_week_costs()` :975），无记录时显示引导文案
- 文档行 = `render_document_card()` :927：标题（tertiary 按钮）+ ✎（VS Code 打开）+ meta 行 + 副标题

## B4.1 分类页（`render_category_view()` :1106）

```text
┌────────────────────────────────────────────────────────────────────┐
│ 首页 / 项目解剖                                                    │
│ 项目解剖                                                           │
│ 一级框架：公司深度、投资假设、风险分析 · 共 31 篇                  │
│ 【仅 02_deals】（app.py:1122-1141）                                │
│ 赛道:[全部▾]  状态:[全部▾]  排序:[按更新时间▾]   命中 18 / 31 篇   │
│ 文档行（doclist）× N                                               │
└────────────────────────────────────────────────────────────────────┘

```

## B4.2 搜索结果页（`render_search_results()` :1167）

```text
┌────────────────────────────────────────────────────────────────────┐
│ 首页 / 搜索：ABF                                                   │
│ 搜索：ABF                                                          │
│ 共 3 个结果                                                        │
│  文档行 × 3（含 ✎ 编辑按钮）                                       │
│ 空态：「未找到匹配结果，尝试其他关键词」（st.info）                │
└────────────────────────────────────────────────────────────────────┘

```

## B4.3 文档详情页（`render_document_detail()` :1188）

```text
┌────────────────────────────────────────────────────────────────────┐
│ 首页 / 项目解剖 / 伏塔_AI驱动电池研发平台                          │
│ 伏塔_AI驱动电池研发平台                                      ✎ 编辑│
│ 项目解剖 · 光计算 · 2026-07-15 · 推进中 · 伏塔                     │
│ ─────────────────────────────────────────────────────────          │
│ 【正文列：Serif 阅读排版，限宽 46rem，行高 1.85】                  │
│ 表格：表头底色 + 斑马纹 + hover；blockquote：2px Navy 左边线 Serif │
│ 图片：限宽 70% / 420px，本地图自动转 base64 内联                   │
│ <details> 折叠块（原文全文）：正文后单独渲染，不进目录             │
│ ─────────────────────────────────────────────────────────          │
│ ← 上一篇                    下一篇 →                               │
│ ▸ 相关文档推荐（5 篇）  ▸ 文件信息                                 │
│ ↑ 回到顶部                                                         │
└────────────────────────────────────────────────────────────────────┘

```

- ✎ 按钮 `os.system('code ...')` 用 VS Code 打开源 md（:921）
- 打开新文档自动滚动回顶（components.html 注入 :1199）

## B5. 论文之战（`render_battle()` battle.py:414）

```text
┌────────────────────────────────────────────────────────────────────┐
│ ⚔️ Thesis Battle                                                   │
│ AI 扮演投委会红队，在限定方法论下攻击你的观点……                    │
│                                                                    │
│ 观点来源文档: [🔬 [项目解剖] 伏塔_AI驱动电池研发平台          ▾]   │
│                                                                    │
│ ▸ 📊 当前沉淀状态与历史日志（expander，读文档「⚔️ Battle 记录」节）│
│                                                                    │
│ 【有预览时】┌──────────────────────────────────────────────────┐   │
│ │ 📋 写回预览（待审批）—— 以下内容经你确认后才会写入文档       │   │
│ │ （渲染后的 Battle 状态表 + 日志）                            │   │
│ │ [✅ 确认写回]  [❌ 取消]                                     │   │
│ └──────────────────────────────────────────────────────────────┘   │
│                                                                    │
│ [💾 结束今日 Battle 并写回文档] [🛑 中断] [🗑️ 清空对话]            │
│ 说「今天到这里」会生成写回预览，经你确认后才落盘……                 │
│ ─────────────────────────────────────────────────────────          │
│ 💬 对话区                                                          │
│ ┌─ AI ──────────────────────────────────────────┐                  │
│ │ Step 1 · Assumption Attack：你的观点依赖……    │                  │
│ └───────────────────────────────────────────────┘                  │
│ ┌─ 我 ──────────────────────────────────────────┐                  │
│ │ 我的证据是……                                  │                  │
│ └───────────────────────────────────────────────┘                  │
│ 空态：「红队已就位」[🔴 让 AI 先开火]                              │
│ ┌──────────────────────────────────────────────────────────────┐   │
│ │ 为你的观点辩护、抛出新证据，或说「今天到这里」收工……        │    │
│ └──────────────────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────────────────┘

```

工程要点：对话按文档持久化（`_session_file` :80）；AI 回复走后台线程（`_reply_worker` :169），可中断，pending 文件防重启丢回复（:450-467）；写回必须审批，「中断」绝不写回。

## B6. 新项目评审（`render_review()` review.py:413 + 行业认知嵌入 app.py:1149）

```text
┌────────────────────────────────────────────────────────────────────┐
│ 🧭 新项目评审                                                      │
│ ① 新项目评审                                                       │
│ ┌─────────────────────────────┬────────────────────────────────┐   │
│ │ 🔍 搜索项目                 │ 🔍 搜索行业总文档              │   │
│ │ 选择评审项目（02_deals）[▾] │ 选择行业总文档(01_industry)[▾] │   │
│ └─────────────────────────────┴────────────────────────────────┘   │
│ [🔮 AI 生成评审草稿]（后台线程，显示进度；中断后可重发）           │
│                                                                    │
│ 【草稿生成后】                                                     │
│ 评审草稿（两段长文本可编辑，确认后写入两个文档）                   │
│ ┌──────────────────────────────────────────────────────────────┐   │
│ │ 项目判断（写入项目文档）—— text_area 可编辑                  │   │
│ └──────────────────────────────────────────────────────────────┘   │
│ 坐标落位：维度A `…` ｜ 维度B `…`    行业阶段更新：…                │
│ 反哺判断：……                                                       │
│ ┌──────────────────────────────────────────────────────────────┐   │
│ │ 增量思考（写入行业总文档 · N.3 评审留档）—— text_area        │   │
│ └──────────────────────────────────────────────────────────────┘   │
│ [💾 确认写入两个文档]                                              │
│ [⚔️ 下一步：对该文档发起 Battle]（跳转 battle 页）                 │
│ ─────────────────────────────────────────────────────────          │
│ ② 行业认知（01_industry 全量文档行列表）                           │
└────────────────────────────────────────────────────────────────────┘

```

- 写回项目文档：替换「🧭 行业总文档评审」节 / 追加表格行（review.py:292-361）
- 写回行业总文档：坐标落位 + 变量更新 + 评审留档（:362）

## B7. 投资雷达（`render_radar()` radar.py:483，5 个 Tab）

```text
┌────────────────────────────────────────────────────────────────────┐
│ 📡 Investment Radar                                                │
│ [📥 信息采集] [🧭 主题叙事] [⚡ 边际变量] [📰 周报生成] [📱 公众号]│
└────────────────────────────────────────────────────────────────────┘

```

### Tab1 信息采集（:496-579）

```text
┌─────────────────────────────────────────────────────────────────────┐
│ [🔄 立即抓取（全量）] [⚡ 重点主题短跑]  上次自动抓取：2026-08-20   │
│ 【运行中】任务进度 steps：✓ 主题A抓取 → ● 主题B分析 → ○ …           │
│ 中断恢复提示：「上次自动任务被中断……可重新发起」                    │
│ 信号 N 条 │ 本周新增 N │ 覆盖主题 N（3 个 metric）                  │
│ 信号池（🤖 = 自动抓取）              按主题筛选: [全部 ▾]           │
│ ┌────────────────────────────────────────────────────────────┐ 🗑️   │
│ │ [08-20] 主题 · 事件类型 · 标题 · 为什么可能改变市场认知    │      │
│ └────────────────────────────────────────────────────────────┘      │
│ 空态：「信号池为空，点击上方按钮开始首次自动抓取」                  │
└─────────────────────────────────────────────────────────────────────┘

```

### Tab2 主题叙事（:581-635）

```text
┌────────────────────────────────────────────────────────────────────┐
│ 选择主题: [光计算 ▾]                    [🔄 更新认知]              │
│ 【有认知记录时】当前叙事一段                                       │
│ ┌────────────────┐ ┌────────────────┐                              │
│ │ 支持证据       │ │ 市场共识       │                              │
│ └────────────────┘ └────────────────┘                              │
│ ┌────────────────┐ ┌────────────────┐                              │
│ │ 代表性观点     │ │ 核心分歧       │                              │
│ └────────────────┘ └────────────────┘                              │
│ 叙事演变历史                                                       │
│ ▸ [08-13] 标志  过去：… 之后：… 触发：… 含义：…                    │
│ 空态：「该主题还没有认知记录——跑一次自动抓取……」                   │
└────────────────────────────────────────────────────────────────────┘

```

### Tab3 边际变量（:638-653）

```text
┌────────────────────────────────────────────────────────────────────┐
│ 边际变量库（AI 从信号中自动提炼，🤖 标记）                         │
│ ▸ [08-20] 🤖 变量标题                                              │
│    边际变量：…  新增信息：…  预期变化：… → …  市场影响：…          │
│    🎯 关联标的（AI 推导，需人工核实）：…                           │
│ 空态：「尚无边际变量——自动抓取后由 AI 提炼……」                     │
└────────────────────────────────────────────────────────────────────┘

```

### Tab4 周报生成（:656-682）

```text
┌────────────────────────────────────────────────────────────────────┐
│ 生成 Weekly Market Intelligence Report                             │
│ 起始日期 [2026-08-14]   结束日期 [2026-08-21]                      │
│ ┌──────────────────────────────────────────────────────────────┐   │
│ │ 关键问题（text_area）：本周最该回答的问题……                  │   │
│ └──────────────────────────────────────────────────────────────┘   │
│ [📝 生成周报]                                                      │
│ ▸ 📋 预览周报（markdown 渲染）                                     │
│ [💾 写入知识库（05_tracking）]                                     │
└────────────────────────────────────────────────────────────────────┘

```

### Tab5 公众号（:685-766）

```text
┌────────────────────────────────────────────────────────────────────┐
│ 公众号文章 → 技术提取（沉淀到 09_tech 技术档案）                   │
│ ┌──────────────────────────────────────────────────────────────┐   │
│ │ 文章 URL（每行一个，text_area）                              │   │
│ └──────────────────────────────────────────────────────────────┘   │
│ ▸ 或直接粘贴正文（单篇）：标题 / 公众号名 / 正文                   │
│ [🧪 技术提取]（后台任务：抓取 → 逐篇提取 → 进度 steps）            │
│ 【提取完成】提取 N 篇，确认后合并进技术档案：                      │
│ ▸ 🏷️《文章标题》→ 落档文件名    定位：… 原理：… 要点：…            │
│ [💾 合并入库（09_tech）]                                           │
└────────────────────────────────────────────────────────────────────┘

```

## B8. 文件归档（`render_ingest()` ingest.py:1020）

```text
┌────────────────────────────────────────────────────────────────────┐
│ 📥 文件归档                                                        │
│ 拖入 PDF / DOCX / TXT / MD，AI 读取内容、提议分类并整理成结构化文档│
│ ┌──────────────────────────────────────────────────────────────┐   │
│ │              拖入文件（可多选，单文件 200MB）                │   │
│ └──────────────────────────────────────────────────────────────┘   │
│ 已选 3 个文件 · 共 24.6 MB                                         │
│ [🔍 开始分析]                                                      │
│                                                                    │
│ 【分析中】📄 BP.pdf  ✓ 解析 → ✓ 分类 → ● 整理  预计剩余 1m20s      │
│ 【中断恢复】「上次后台分析被中断……」[🔄 重新发起分析][🗑 清除记录]  │
│                                                                    │
│ 【分析完成】分类完成：2 可归档 / 3 总计                            │
│ ┌─────────────────────────────┬────────────────────────────────┐   │
│ │ 📄 BP.pdf  摘要/关键词      │ 分类（可改判）: [项目解剖 ▾]   │   │
│ └─────────────────────────────┴────────────────────────────────┘   │
│ [💾 确认分类，整理入库]                                            │
│                                                                    │
│ 【入库完成】本次归档：2 成功 / 3 总计                              │
│ ✓ BP.pdf → 02_deals/伏塔_xx.md            [🧭 发起评审]            │
│ ❌ 访谈.docx：解析失败原因                                         │
│ 已入库的文档可在左侧分类或搜索中查看。                             │
└────────────────────────────────────────────────────────────────────┘

```

- 两阶段后台任务：分析（`_run_job` :784）→ 入库（`_run_save_job` :894），各带 job 文件断点与 ETA（:721-752）
- 上传先落缓存（`_cache_uploads` :672），中断后不用重新拖文件即可重发
- 入库成功卡片直接给「🧭 发起评审」入口，跳 compare 页

## B9. 组件清单（交互版）

```text
Layout:   TopNav（fixed 浮层导航）/ SidebarPanel / Breadcrumb / SectionHeader
Content:  StatFlex（首页三栏统计）/ CatBlock（分类块）/ DocRow（行式文档卡）
          DocBody（Serif 阅读列）/ PrevNext / TocSpy（scroll-spy 目录）
AI 交互:  ChatBattle（对话区）/ WritebackPreview（写回审批卡）
          JobSteps（后台任务进度 + ETA）/ InterruptedBanner（中断恢复）
Radar:    SignalCard / ThemeQuad（四象限卡）/ VarExpander / WeeklyReport
Ingest:   UploadZone / ClassifyRow（文件卡 + 可改判分类下拉）/ ResultRow
设置:     ProviderPanel（厂家/端点/模型/Key + 本机持久化）
```

## B10. 设计 token 与工程约定

设计 token（Editorial Research Terminal，app.py:431-454）：

```css
:root {
  --kb-bg: #F5F4F1;      /* 暖纸底 */
  --kb-card: #FFFFFF;
  --kb-text: #202428;    /* 二级 #626970 / 三级 #91979D */
  --kb-accent: #314A63;  /* Navy */
  --kb-accent-soft: #EEF1F4;
  --kb-border: #DDDDD8;
  --kb-radius: 6px;      /* 无阴影 */
  /* UI: Inter + Noto Sans SC ｜ 正文: Noto Serif SC ｜ 数字: IBM Plex Mono */
}
/* 排版语言：细线分隔、行式列表、小编辑标（.section-header），不用大白卡 */
```

工程约定（改写时别丢）：

```text
1. 所有 AI 任务走后台线程 + data/ 下 job 文件，刷新/切页自愈，中断可恢复
2. 写回知识库一律「预览 → 人工确认」两阶段，AI 不直接改文档
3. API Key / 厂家 / 模型 / 端点持久化到本机 data/local_*.txt（外发前必须删 key）
4. AI 调用成本写 data/ai_costs.jsonl，首页展示近 7 天聚合
5. ✎ 按钮直接调本机 VS Code 打开源文件
```

---

# Part C · 全局约定与改写要点

## C1. 状态与数据约定

```text
文档状态（tracks.py 归一）：跟踪中 / 推进中 / 已投资 / 已放弃 / 已退出
赛道（track）：关键词表从 文件名+标题 推断
分类体系（config.py:22 CATEGORY_MAP）：
  01_industry 行业认知 / 02_deals 项目解剖 / 03_frameworks 方法论
  04_comparables 横向比较 / 05_tracking 动态追踪 / 06_strategy 投资策略
  07_learnings 经验沉淀 / 08_funds 被投基金 / 09_tech 技术沉淀
知识库源目录优先级：KB_KNOWLEDGE_DIR > knowledge_app/knowledge/ > ~/.kimi/knowledge
```

## C2. 已知脆弱点

```text
1. compare/battle 页靠正则抽 md 章节，文档没写对应章节就空
2. search.js 内嵌全站全文（1MB），文档量翻倍会成大问题
3. 两套前端 Sidebar 都无折叠，分类/目录长了会失控
4. 无统一的 Empty/Error/Toast 组件体系（交互版靠 st.info/warning 散落各处）
5. dist/ 是过期产物（2026-07-27），以 build_html.py 为准，验证需重跑
6. 静态站模板全是 build_html.py 里的 f-string，无独立模板文件
```

## C3. 改写要点速查

```text
· 静态站改样式只动 BASE_CSS（:137-303）；加页面加 build_xxx() + main() 注册
· 交互版改样式只动 app.py 顶部 <style>（:426-816）；加页面改 _TOP_NAV + 分发
· ui_demo/index.html 是独立的新版设计稿（UI Demo v4），可能是目标视觉方向
· 两套前端共享同一套分类/状态/赛道约定，改 config.py / tracks.py 两边都生效
```
